# 实测记录（VERIFICATION.md）

> 本文件记录 Claude Code 版围栏的每一条事实依据与实测结果。平台会变，
> 结论一律标注验证日期与版本——**装机后请跑 `bash loopwork/scripts/selftest.sh` 用你自己的版本重测**。
> 历史结论保留，不删不改：删掉的实测记录等于没做过实测。

## 当前实测环境

- 日期：**2026-09-11**
- 平台：Claude Code（macOS）
- 无 OS 沙箱——本版的物理边界只有钩子覆盖面，这一点不假装等价（对照 Codex 版有 Seatbelt）

## 平台事实

| # | 项目 | 结论 | 证据 |
|---|---|---|---|
| 1 | **Stop 钩子连续顶回有硬上限** | **有**。`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` 默认 **8**：连顶 8 次后平台强制放行，钩子拦不住，`batch.flag` 会残留 | 因此围栏自设 `MAX_BLOCKS = 7`，抢在平台之前优雅停批并摘 flag；对照 Codex 实测无上限（连顶 12 次仍生效） |
| 2 | **优雅停机需要一次「放行」** | 摘 flag 那一轮仍顶回一次把话说完，同时写 `stop_blocks = -1`；**下一次 Stop 无条件放行**，掐断平台的连续顶回计数 | 真项目实测：第 7 次顶回摘 flag 后立刻重开批，紧接的那一轮确实被放行（不是 bug，是哨兵在干活） |
| 3 | **PreToolUse 看得见命令行，看不见执行结果** | 因此「有没有落过红存档」这张票**必须从 git 历史里读**，不能存在状态文件里——存起来的标记，一次故意失败的 commit 就能白拿一张票 | 这条决定了 `guard_bash.py` 的实现取向（见下） |
| 4 | **`git commit` 的实际载荷 = 已暂存 ∪ 命令行会暂存的** | 只看 `git diff --cached` 会被 `git add -A && git commit` 和 `git commit -am` 整个绕过 | 真项目实测：两条路径都被存档闸拦下（见下表） |

## 三层围栏

```
第一层 —（无 OS 沙箱，这是与 Codex 版的实质差距，不遮掩）
第二层 实时围栏  PreToolUse：guard_edits.py 锁文件（实现期 tests/ spec.md rules.md 只读）
                 guard_bash.py 看命令行——含存档闸（先红后绿）与 shell 绕路（sed -i / heredoc / 解释器一行程序）
第三层 检测门    Stop 钩子 stop_batch.py 轮末快检：基线对账 + 相位纪律 + JOURNAL/审计只增不减
                 自带 MAX_BLOCKS = 7 刹车（平台事实 #1）
审计             PostToolUse 全量日志 .loopwork/logs/audit.jsonl（只增不减）
                 拦截取证 .loopwork/logs/blocks.jsonl → 轮末汇成一句「本轮撞墙 N 次」
```

## 真项目跑通记录 · 2026-09-11（不是单元测试，是拿一个空目录从头做完一轮）

在一个临时项目（`行前缀计数器`，规格两句）上，用**装出来的钩子**跑完整条流水线。

| 步骤 | 观察到的 | 结论 |
|---|---|---|
| `init_project.sh <目录> <项目名>` | 状态机 + 钩子 + `.claude/settings.json` 接线 + 首次存档一次到位 | 建家可用 |
| 实现期改 `tests/test_tally.py` | `exit 2`「考题已锁定」 | 考题锁有效 |
| 实现期改 `spec.md` | `exit 2` | 规格锁有效 |
| **跳过红存档直接存实现** | `git add -A && git commit …` → `exit 2`「上一次绿存档之后没有过红存档」 | **存档闸拦住了「先绿后红」** |
| 同上换 `git commit -am …` | 同样 `exit 2` | 载荷算法覆盖两条暂存路径（平台事实 #4） |
| 撤实现、补红存档 | 只含 `tests/` + 台账的 commit **放行**（`b3e7641`） | 红档白名单正确 |
| 补票后再存实现 | **放行**，落档 `a282e98` | 有票即通，不是一刀切 |
| 开批第 1 轮 | 顶回话尾自动挂上「[取证] 本轮实时围栏拦下 4 次动作」 | 证据漏斗在真项目里生效 |
| 连开 7 轮（每轮有进展） | 第 7 次「顶回已达安全上限（7 次），先于平台硬上限优雅停批」+ **自动摘 flag** | 平台事实 #1 的刹车真的踩住了 |
| 空转两轮 | 第 1 次警告；第 2 次「连续 2 次顶回一点进展都没有（仍是第 1 轮，HEAD、tasks.md、BLOCKED.md 都没动过）」+ 自动停批 | 停滞判定四条件生效 |
| 空转一轮后只动 `BLOCKED.md` | 进展指纹变化，`stalls` 归 0，不计打转 | **一条硬任务跨两次顶回不再被误判** |

**一条已知的良性残留**：轮末 `set last_round_commit` 之后 `git status` 不干净——
**一个 commit 装不下自己的 hash**，这是设计固有的，两版对称。下一次存档会顺手带上，不作处理。

## 围栏管不到的地方（同样写在 README）

- 用户自己终端里敲的命令不经过围栏——这是特性：关批的开关只在用户手上，模型删不掉 `batch.flag`；
- `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP=0` 可以关掉平台顶回，围栏的检测门随之失效；
- 解释器一行程序、变量间接（`X=tests; sed -i "" … $X/a.py`）这类写法，命令行匹配面永远有缝——
  所以第三层检测门是必需的，不是冗余：绕过实时围栏改了考题，轮末照样对账翻出来。
